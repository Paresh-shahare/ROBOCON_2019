#include <EEPROM.h>    //library for accessing arduino EEPROM
#include "array.h"
#include "EEPROM_funcs.h"

void setup() {

  Serial.begin(9600);
//  E_clean();  //call this to clean EEPRROM memory 
//  E_write();  //call this to write to the EEPROM memory
//  E_read();   //call this to read the data stored in EEPROM
}

void loop() {
//Nothing to see here.
}
